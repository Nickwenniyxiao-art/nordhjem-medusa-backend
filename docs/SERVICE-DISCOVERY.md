# SERVICE DISCOVERY

> 项目名称: [项目名称]  
> 创建日期: [YYYY-MM-DD]  
> 状态: Template  
> 负责人: [负责人]

## 概述 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义服务发现体系的设计目标、适用场景与核心组件，确保服务可动态寻址与高可用通信。
应说明注册中心类型（DNS/Consul/etcd/Kubernetes Service）及一致性策略。
建议明确静态发现与动态发现的边界，避免多种机制混用导致复杂性上升。
需给出命名规范、实例元数据字段与 TTL 策略。
最佳实践：将服务发现配置纳入 IaC，避免手工注册造成漂移。

| 组件 | 职责 | 可用性目标 | 备注 |
|---|---|---|---|
| [注册中心] | [实例登记与查询] | [99.99%] | [多可用区部署] |

## 注册机制 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节描述服务实例上线、续约、下线的注册流程与接口约束。
应明确实例启动时注册顺序：健康探针就绪后再对外可见，避免冷启动失败流量。
建议记录注册负载字段，如版本、区域、权重、能力标签。
需定义注册失败重试策略、退避算法与幂等处理。
最佳实践：使用 sidecar 或平台控制器托管注册逻辑，减少业务代码侵入。

1. [实例启动并初始化依赖]
2. [执行 readiness 检查]
3. [向注册中心提交实例元数据]
4. [注册成功后接受流量]

## 发现机制 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义客户端发现与服务端发现模式的选择依据及实现规范。
应说明负载均衡算法（轮询/最少连接/区域优先）和缓存刷新策略。
建议区分强一致查询与最终一致查询场景，平衡性能与准确性。
需描述网络分区、缓存过期和旧实例污染时的处理机制。
最佳实践：关键路径优先使用本地缓存 + 后台刷新，降低注册中心压力。

| 发现模式 | 适用场景 | 优点 | 风险控制 |
|---|---|---|---|
| [客户端发现] | [高性能内部调用] | [低延迟] | [本地缓存TTL与熔断] |

## 健康检查 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义 liveness/readiness/startup 探针标准及判定阈值。
应规定健康检查必须覆盖关键依赖（数据库、消息队列、下游核心服务）而非仅进程存活。
建议设置分级健康状态（Healthy/Degraded/Unhealthy），支持细粒度流量调度。
需记录失败阈值、恢复阈值、检测间隔与隔离时长。
最佳实践：健康检查逻辑保持轻量，避免检查本身成为负载瓶颈。
健康检查清单：

- [ ] 探针超时小于调用超时
- [ ] 失败阈值经过压测验证
- [ ] 退化状态可触发降级策略
- [ ] 监控面板可视化状态分布

## 故障处理 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节说明服务发现异常时的故障处置策略，如注册中心不可用、脑裂、实例抖动。
应定义自动化降级动作，包括使用本地缓存、静态兜底地址、只读模式。
建议建立故障演练剧本，覆盖 DNS 污染、配置错误、跨区网络抖动等典型场景。
需记录 MTTR 目标、升级路径与跨团队协作机制。
最佳实践：故障后必须产出复盘并更新发现策略与告警阈值。

| 故障类型 | 检测信号 | 自动处置 | 人工处置 |
|---|---|---|---|
| [注册中心超时] | [查询延迟飙升] | [切本地缓存] | [排查网络与集群状态] |
