# 嵌入式 Bootloader 设计

> 项目名称: [项目名称]  
> 创建日期: [YYYY-MM-DD]  
> 状态: Template  
> 负责人: [负责人]

## 概述

- 说明 Bootloader 在设备启动链中的定位、适用芯片平台与安全目标。
- 描述 [设备类型]、[部署环境]、[受监管标准] 对设计的约束。
- 约定术语：Primary Slot、Recovery Slot、Secure Element、Anti-rollback Counter。
- 给出版本策略：语义化版本 + [硬件代号] + [编译时间戳]。
- 明确成功判定：启动时延、认证通过率、升级失败恢复率等 KPI。

| 字段 | 说明 | 示例 |
| --- | --- | --- |
| [目标系统] | [Bootloader 设计范围与边界] | [ARM Cortex-M33 + TrustZone] |

流程步骤：
1. [步骤 1：填写关键动作与责任人。]
2. [步骤 2：定义输入、输出与验收标准。]
3. [步骤 3：记录证据链接并完成审批。]

检查清单：

- [ ] [是否已补全本章节所有方括号占位符。]
- [ ] [是否已标注版本、日期与审批记录。]
- [ ] [是否已将文档链接同步到项目索引。]

## 启动流程

- 定义上电复位后 ROM → Bootloader → 应用镜像 的执行顺序。
- 描述关键决策点：镜像选择、签名验证、版本比较、看门狗喂狗策略。
- 要求绘制状态机并标注每个状态超时阈值与错误码。
- 加入硬件自检流程，例如时钟、Flash、加密外设可用性检测。
- 规定日志等级与串口/调试口输出规范，避免泄露敏感信息。

| 阶段 | 输入 | 输出 |
| --- | --- | --- |
| [ROM 初始化] | [复位向量/熔丝配置] | [跳转到 Bootloader 主入口] |

流程步骤：
1. [步骤 1：填写关键动作与责任人。]
2. [步骤 2：定义输入、输出与验收标准。]
3. [步骤 3：记录证据链接并完成审批。]

检查清单：

- [ ] [是否已补全本章节所有方括号占位符。]
- [ ] [是否已标注版本、日期与审批记录。]
- [ ] [是否已将文档链接同步到项目索引。]

## 安全校验

- 采用 [签名算法]（如 ECDSA P-256）验证镜像完整性与来源。
- 描述证书链管理：Root Key、Device Key、轮换窗口与吊销列表。
- 要求校验固件元数据：版本号、硬件兼容矩阵、最小安全版本。
- 定义反回滚策略：单调计数器、OTP/eFuse 存储、异常写保护。
- 规定失败处置：进入 Recovery Mode 并触发 [告警渠道]。

| 校验项 | 实现方式 | 失败动作 |
| --- | --- | --- |
| [镜像签名] | [公钥验签 + 哈希比对] | [拒绝启动并记录审计日志] |

流程步骤：
1. [步骤 1：填写关键动作与责任人。]
2. [步骤 2：定义输入、输出与验收标准。]
3. [步骤 3：记录证据链接并完成审批。]

检查清单：

- [ ] [是否已补全本章节所有方括号占位符。]
- [ ] [是否已标注版本、日期与审批记录。]
- [ ] [是否已将文档链接同步到项目索引。]

## 升级支持

- 区分 OTA、USB、串口三类升级通道，并定义认证要求。
- 明确 A/B 分区或单分区 + 暂存区策略与容量预算。
- 给出升级包格式模板（头部、分片、摘要、签名、兼容信息）。
- 描述断点续传、分片重传和升级超时的处理机制。
- 要求升级前后执行健康检查并写入 [设备管理平台]。

| 升级模式 | 触发条件 | 回执 |
| --- | --- | --- |
| [OTA 增量升级] | [[云端下发任务]] | [[任务 ID + 成功/失败码]] |

流程步骤：
1. [步骤 1：填写关键动作与责任人。]
2. [步骤 2：定义输入、输出与验收标准。]
3. [步骤 3：记录证据链接并完成审批。]

检查清单：

- [ ] [是否已补全本章节所有方括号占位符。]
- [ ] [是否已标注版本、日期与审批记录。]
- [ ] [是否已将文档链接同步到项目索引。]

## 回滚方案

- 定义回滚触发条件：启动失败次数阈值、核心服务健康探针失败等。
- 描述回滚执行步骤：标记当前槽失败、切换到上一个可用镜像、验证后恢复服务。
- 规定回滚后事件上报字段：设备 ID、失败版本、恢复版本、时间戳。
- 要求提供人工介入流程与远程锁定机制，防止重复失败循环。
- 约定演练频率：每 [季度] 至少一次故障注入与回滚验证。

| 触发场景 | 自动动作 | 人工动作 |
| --- | --- | --- |
| [新固件启动连续失败 3 次] | [切换到上个稳定版本] | [研发确认根因并冻结发布] |

流程步骤：
1. [步骤 1：填写关键动作与责任人。]
2. [步骤 2：定义输入、输出与验收标准。]
3. [步骤 3：记录证据链接并完成审批。]

检查清单：

- [ ] [是否已补全本章节所有方括号占位符。]
- [ ] [是否已标注版本、日期与审批记录。]
- [ ] [是否已将文档链接同步到项目索引。]

