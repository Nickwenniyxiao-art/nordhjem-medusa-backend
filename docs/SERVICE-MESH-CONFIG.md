# SERVICE MESH CONFIG

> 项目名称: [项目名称]  
> 创建日期: [YYYY-MM-DD]  
> 状态: Template  
> 负责人: [负责人]

## 概述 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义服务网格配置文档的目标与治理原则，覆盖流量、安全、观测与发布控制。
应说明适用环境（开发/测试/生产）与配置来源（GitOps/控制平面/策略中心）。
建议明确网格内外流量边界、Sidecar 注入策略与命名空间准入规则。
需定义配置变更审核机制，确保高风险策略可追踪、可回滚。
最佳实践：将网格配置视为代码（Policy as Code），统一走 PR 与自动化校验。

| 配置域 | 管理对象 | 生效范围 | 回滚方式 |
|---|---|---|---|
| [TrafficPolicy] | [VirtualService] | [namespace/service] | [git revert] |

## 流量治理 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节记录路由、超时、重试、熔断、流量镜像等流量治理策略。
应为关键接口定义基线参数，并区分读流量与写流量策略差异。
建议引入金丝雀发布与按标签路由，支持版本灰度与实验验证。
需配置异常流量防护（突增、慢请求、重试风暴）和紧急熔断手段。
最佳实践：每次变更后执行合成流量验证并确认 SLO 未退化。

| 策略项 | 默认值 | 适用服务 | 例外条件 |
|---|---|---|---|
| [请求超时] | [500ms] | [checkout-*] | [批处理接口可放宽] |

## 安全策略 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义 mTLS、身份认证、授权策略、证书轮换与东西向访问控制。
应明确服务身份来源（SPIFFE/SVID 或平台身份）与证书有效期策略。
建议按最小权限原则配置 AuthorizationPolicy，拒绝默认放通。
需记录敏感服务隔离规则及跨命名空间访问审批机制。
最佳实践：建立证书到期预警与自动轮换演练，避免生产中断。
安全检查清单：

- [ ] mTLS 在生产环境强制开启
- [ ] 默认拒绝未授权调用
- [ ] 高敏服务有额外访问门禁
- [ ] 证书轮换演练已通过

## 可观测性配置 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节说明网格层指标、日志、追踪配置及采样策略。
应统一定义请求 ID 透传规则与链路追踪字段标准。
建议按服务等级设置采样率，关键链路高采样、低价值流量低采样。
需明确指标保留周期、日志脱敏要求与告警阈值。
最佳实践：将 SLI 映射到网格指标，形成“配置—观测—告警”闭环。

| 观测项 | 数据源 | 阈值 | 告警渠道 |
|---|---|---|---|
| [5xx 比例] | [Prometheus] | [>1% 持续5m] | [PagerDuty/IM] |

## 发布策略 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义服务网格支持的发布策略，如蓝绿、金丝雀、按租户灰度。
应标注每种策略适用场景、风险等级与回滚触发条件。
建议配套发布前检查（配置校验、容量评估、依赖健康）与发布后观察窗口。
需定义失败回退自动化脚本与人工应急流程。
最佳实践：将发布策略模板化，减少手工配置差异导致的事故。

1. [发布前：完成策略静态校验与审批]
2. [发布中：按比例放量并实时监控关键指标]
3. [发布后：确认无回归并冻结配置变更窗口]
4. [异常时：一键回滚至稳定版本]
