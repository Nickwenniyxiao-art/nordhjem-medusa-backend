# SERVICE CONTRACT

> 项目名称: [项目名称]  
> 创建日期: [YYYY-MM-DD]  
> 状态: Template  
> 负责人: [负责人]

## 概述 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义服务契约文档的目的：保障服务协作稳定、降低跨团队集成成本、支持独立演进。
应说明契约适用范围（同步 API、异步事件、批处理接口）以及优先级规则。
建议将契约视为产品级资产，要求版本化管理并纳入代码评审。
应明确消费者责任，例如显式处理新增字段、超时、幂等与错误码分支。
最佳实践：契约变更前先进行 Consumer-Driven Contract（CDC）验证，减少兼容性事故。

| 契约对象 | 表达格式 | 存储位置 | 审核频率 |
|---|---|---|---|
| [订单API] | [OpenAPI 3.1] | [repo/path] | [每次发布] |

## 接口定义 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节描述接口资源、方法、URI、鉴权模式、幂等策略与错误处理语义。
每个接口应给出请求示例、响应示例、状态码矩阵与异常场景说明。
建议标注字段约束（必填/可空/枚举）和业务含义，避免仅给技术定义。
应记录性能预算，如 p95 延迟、最大 payload、并发限制。
最佳实践：所有接口定义应可被机器校验（lint + schema check），并自动生成 SDK。

| 接口名 | 方法 | 路径 | 鉴权 | 幂等键 | 说明 |
|---|---|---|---|---|---|
| [CreateOrder] | [POST] | [/v1/orders] | [JWT] | [Idempotency-Key] | [创建订单] |

## 输入输出约束 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节用于定义输入输出数据约束，包括格式、取值范围、默认值和错误返回规范。
应明确不兼容输入处理策略（拒绝/纠正/降级）与审计记录要求。
建议提供字段级规则表和跨字段校验规则（如日期区间、金额一致性）。
输出约束应覆盖分页、排序、过滤、脱敏字段与多语言处理。
最佳实践：用 JSON Schema 或 Protobuf 约束接口数据，并在 CI 中执行契约测试。

| 字段 | 类型 | 约束 | 默认值 | 违规处理 |
|---|---|---|---|---|
| [currency] | [string] | [ISO-4217] | [CNY] | [400 INVALID_CURRENCY] |

## 版本兼容策略 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义版本策略（URI 版本、Header 版本或 schema 版本）及兼容窗口。
应规定允许的变更类型（新增可选字段、扩展枚举）与禁止变更类型（删除字段、语义反转）。
建议采用语义化版本并记录升级路径（N→N+1），保障消费者平滑迁移。
需包含废弃策略：公告周期、双写周期、停用日期与迁移指南。
最佳实践：对 Breaking Change 强制进行影响面扫描并由架构评审批准。
兼容性检查清单：

- [ ] 新增字段为可选
- [ ] 旧字段保留兼容
- [ ] 枚举扩展已通知消费者
- [ ] 停用计划已发布

## 变更流程 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节记录契约变更从提案到发布的标准流程，确保可追溯与可回滚。
建议流程包含：变更申请、影响评估、消费者确认、灰度发布、验收关闭。
应定义紧急变更路径与补录要求，防止高压场景下治理失效。
每次变更必须绑定工单与发布记录，沉淀审计证据。
最佳实践：变更完成后组织复盘，更新契约规范和反模式案例库。

1. [提交契约变更提案并附影响清单]
2. [执行自动化契约测试与回归测试]
3. [灰度发布并监控错误码/延迟]
4. [完成消费者验收并归档]
