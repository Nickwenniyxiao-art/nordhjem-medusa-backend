> 项目名称: [项目名称]  
> 创建日期: [YYYY-MM-DD]  
> 状态: Template  
> 负责人: [负责人]

## 概述 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义熔断策略的业务目标：限制故障扩散、保护核心服务、提升整体系统稳定性。
应描述适用调用链（同步 API、消息消费、第三方依赖）与策略分层（网关/服务/客户端）。
建议明确熔断、限流、隔离、重试之间的关系，避免策略冲突。
需说明策略配置来源与生效范围，支持按服务、接口、租户粒度控制。
最佳实践：将熔断参数与压测基线绑定，避免拍脑袋设置阈值。

| 策略层级 | 控制对象 | 默认状态 | 责任团队 |
|---|---|---|---|
| [客户端熔断] | [HTTP/gRPC 调用] | [启用] | [平台团队] |

## 触发条件 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节规定熔断触发依据，包括错误率、超时率、并发队列饱和、延迟异常。
应区分瞬时抖动与持续性故障，避免因短时波动误触发。
建议采用滑动窗口与最小请求阈值，确保统计显著性。
需定义不同优先级接口的触发阈值分层策略。
最佳实践：将触发条件与 SLO 预算联动，做到可解释、可审计。

| 指标 | 触发阈值 | 窗口 | 最小样本 | 备注 |
|---|---|---|---|---|
| [5xx错误率] | [>=20%] | [30s] | [100] | [核心链路更严格] |

## 降级策略 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节描述熔断后系统如何降级，确保关键业务“可用但受限”。
应为不同业务定义降级级别：静态兜底、缓存数据、延迟处理、只读模式。
建议明确不可降级能力（如支付扣款）与替代流程（人工审核/队列补偿）。
需记录用户侧提示策略，减少体验损伤与客服压力。
最佳实践：降级策略必须可演练、可监控、可自动恢复。
降级检查清单：
- [ ] 兜底数据来源可信
- [ ] 降级路径已压测
- [ ] 用户提示文案已评审
- [ ] 关键交易具备补偿机制

## 恢复机制 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义从 Open 到 Half-Open 再到 Closed 的恢复流程与判定标准。
应说明探测请求比例、恢复窗口和回退条件，防止抖动式开合。
建议采用渐进恢复（10%→30%→100%）并监控关键指标。
需规定恢复失败时的升级处理与人工接管阈值。
最佳实践：恢复策略应与发布节奏解耦，避免变更窗口叠加风险。

1. [熔断后进入观察窗口]
2. [按探测比例发送试探请求]
3. [指标稳定后逐级放量]
4. [恢复失败则回退并升级告警]

## 监控指标 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义熔断治理必须追踪的指标、告警规则与看板结构。
应包含熔断触发次数、持续时长、降级命中率、恢复成功率与用户影响面。
建议按服务/接口/租户维度切片分析，定位热点故障源。
需配置异常模式检测，如“反复开关”或“长期半开”并触发专项排查。
最佳实践：每次重大事件后复核指标体系，持续优化阈值。

| 指标名称 | 目标值 | 告警阈值 | 处置人 |
|---|---|---|---|
| [恢复成功率] | [>95%] | [<80% 持续15m] | [SRE On-call] |
