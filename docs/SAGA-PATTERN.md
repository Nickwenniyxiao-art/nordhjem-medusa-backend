> 项目名称: [项目名称]  
> 创建日期: [YYYY-MM-DD]  
> 状态: Template  
> 负责人: [负责人]

## 概述 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义 Saga 事务编排的适用场景与目标，解决跨服务最终一致性问题。
应说明编排式与协同式 Saga 的选型原则，以及与本系统架构的匹配依据。
建议列出关键业务事务链路（下单、库存预占、支付、发货）及失败影响。
需明确全局事务 ID、状态机模型与审计要求。
最佳实践：优先保证补偿可执行性，再追求流程最短路径。

| 事务名称 | Saga 模式 | 关键服务 | 一致性目标 |
|---|---|---|---|
| [订单履约] | [编排式] | [order/inventory/payment] | [最终一致] |

## 业务流程 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节描述端到端业务流程、步骤顺序、状态迁移与关键决策点。
应明确每一步输入、输出、超时与重试策略，避免隐式依赖。
建议使用流程图 + 表格双表达，兼顾可视化与可执行细节。
需标注外部系统交互点与人工介入节点。
最佳实践：流程定义与执行引擎配置保持一致，防止文档与实现漂移。

| 步骤 | 执行服务 | 输入 | 输出 | 失败处理 |
|---|---|---|---|---|
| [创建订单] | [order-service] | [购物车数据] | [order_id] | [触发取消流程] |

## 本地事务 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义每个服务内部本地事务边界、提交条件与幂等要求。
应确保本地事务与事件发布采用可靠机制（如 Outbox）避免消息丢失。
建议为每个本地事务定义唯一幂等键与重放行为。
需记录数据库锁策略、隔离级别与性能影响。
最佳实践：本地事务保持短小，减少锁持有时间与死锁概率。

| 服务 | 本地事务内容 | 提交条件 | 幂等键 |
|---|---|---|---|
| [inventory] | [库存预占写入] | [库存充足] | [reservation_id] |

## 补偿机制 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节定义补偿动作、触发时机、顺序依赖与幂等保障。
应为每个正向步骤配置对应补偿步骤，确保可逆或可替代。
建议区分技术补偿（回滚数据）与业务补偿（发放优惠券/人工处理）。
需规定补偿失败的重试、死信队列与人工接管机制。
最佳实践：补偿接口同样需要契约测试和压测验证。
补偿检查清单：
- [ ] 每个步骤都有补偿定义
- [ ] 补偿动作幂等
- [ ] 补偿顺序与依赖清晰
- [ ] 补偿失败有升级路径

## 失败处理 — [详细说明该章节应包含的内容、格式要求和最佳实践示例]

本章节说明 Saga 执行失败、超时、重复事件、乱序事件等异常处理策略。
应定义统一错误码与失败分类，支持快速定位根因。
建议设置事务最大执行时长与超时清理任务，防止悬挂事务。
需记录告警规则、人工干预 SOP 与恢复验证标准。
最佳实践：定期做故障注入演练，验证失败处理路径可用性。

| 失败类型 | 自动处理 | 人工处理 | 完成判定 |
|---|---|---|---|
| [支付超时] | [重试3次后补偿] | [客服回访] | [订单状态关闭] |
