---
项目名称: NordHjem Medusa Backend
创建日期: 2026-03-16
状态: 草稿
负责人: [Owner/CTO]
---

# IoT 固件规范

## 概述

本章节应说明固件规范的适用设备、约束目标与合规边界。
建议拆分“运行稳定性、可升级性、安全性”三类目标并绑定指标。
应明确固件包命名、签名方式、发布责任人与审批机制。
最佳实践：固件规范和硬件 BOM 保持版本联动。

| 规范项 | 占位符 |
| --- | --- |
| 适用型号 | [GW-X1/X2] |
| 支持架构 | [ARMv7/ARM64] |
| 发布责任人 | [Firmware Owner] |

## 固件架构

本章节需描述引导层、内核层、应用层、配置层的分层架构与边界。
建议提供模块依赖图和启动时序，便于定位启动故障。
应明确只读分区与可写分区策略，避免配置污染系统分区。
最佳实践：业务逻辑与设备驱动解耦，支持独立升级。

| 层级 | 组件 | 职责 | 可更新性 |
| --- | --- | --- | --- |
| Bootloader | [U-Boot] | [安全启动] | [受控] |
| Application | [Agent] | [业务逻辑] | [可 OTA] |

## 版本策略

本章节应定义语义化版本规则、分支策略、LTS 维护周期与弃用政策。
建议区分“功能版/补丁版/紧急修复版”，并给出发布节奏。
应维护版本兼容矩阵：硬件型号 × 固件版本 × 协议版本。
最佳实践：每个发布版本都绑定变更摘要和回滚点。

| 版本类型 | 编号规则 | 发布频率 | 支持周期 |
| --- | --- | --- | --- |
| Feature | [vX.Y.0] | [月度] | [12个月] |
| Patch | [vX.Y.Z] | [按需] | [继承主版本] |

## 升级兼容性

本章节需定义跨版本升级路径、前置检查与失败回退规则。
建议在升级前执行兼容性探测：存储空间、电量、网络质量、证书状态。
流程建议：
1. [预检]；2. [下载校验]；3. [分区写入]；4. [重启验证]；5. [状态回传]。
最佳实践：对不兼容版本提供“桥接版本”路径，降低砖机风险。

- 升级检查清单：
  - [ ] 电量高于 [30%]
  - [ ] 空闲存储高于 [2x 包大小]
  - [ ] 当前版本在允许升级集合内

## 安全要求

本章节应定义签名验签、安全启动、最小权限、敏感数据保护与漏洞响应流程。
建议采用 [ECDSA/RSA] 签名并强制校验完整性哈希。
应规定禁用默认口令、调试接口保护、密钥硬件隔离。
最佳实践：将 CVE 修复 SLA 纳入发布门禁。

| 控制项 | 要求占位符 | 验证方式 |
| --- | --- | --- |
| 固件签名 | [必须启用] | [CI/CD 验签测试] |
| 安全启动 | [强制] | [启动链路审计] |
| 漏洞修复SLA | [高危7天] | [工单闭环] |

